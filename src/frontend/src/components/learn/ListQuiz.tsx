
/**
 * A component that displays the quizzes that are available. Contains mechanisms
 * to ease the navigation and allows the user to select a quiz.
 */
const ListQuiz = () => {
 
  const fetchQuizzes = () => {

  }

  return (
    <div className="quiz-list">
        
    </div>
  )
}

export default ListQuiz;
