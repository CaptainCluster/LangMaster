import Panel from "./Panel";
const SuggestionsPanel = () => {
  return (
    <Panel header="Suggestions" text="This is where the user gets some suggestions."/>
  );
}

export default SuggestionsPanel;
